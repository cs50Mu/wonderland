#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])   // argc for argument count, and argv is an array of char *strings for
{
	int distance = 100;        // the arguments.

	// this is also a comment
	printf("You are %d miles away.\n", distance);

	return 0;
}
